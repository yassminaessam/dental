// Firebase removed. This module remains as a stub to avoid breaking imports.
// Do not use in new code. Data is now stored in Neon Postgres via Prisma.

export const app = undefined as any;
export const db = undefined as any;
export const storage = undefined as any;
export const auth = undefined as any;
