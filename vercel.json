{
  "version": 2,
  "buildCommand": "prisma generate && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/workflows/process",
      "schedule": "0 * * * *"
    }
  ]
}